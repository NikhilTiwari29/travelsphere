# Registers location-service so Gateway and Feign clients can resolve city/airport APIs.
eureka:
  instance:
    prefer-ip-address: true
    instance-id: ${spring.application.name}:${spring.application.instance_id:${random.value}}
  client:
    register-with-eureka: true
    fetch-registry: true

spring:
  application:
    name: location-service

  # Loads shared database/cache configuration from Config Server when available.
  config:
    import: optional:configserver:http://localhost:8888

  # Redis backs Spring Cache for airport and city lookup data.
  data:
    redis:
      host: ${SPRING_DATA_REDIS_HOST:localhost}
      port: ${SPRING_DATA_REDIS_PORT:6379}
      timeout: 2000ms
      lettuce:
        pool:
          max-active: 8
          max-idle: 4
          min-idle: 1
  cache:
    type: redis

# Metadata exposed through Actuator info.
info:
  app:
    name: ${spring.application.name}
    description: Manages airports and city location data
    version: 1.0.0
    environment: development

# Health/info endpoints confirm location-service readiness.
management:
  endpoints:
    web:
      exposure:
        include: health, info
  endpoint:
    health:
      show-details: always
    info:
      enabled: true
  info:
    env:
      enabled: true
    java:
      enabled: true
    os:
      enabled: true
